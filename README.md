# xstash-cli
A CLI tool to continuously sync your X (Twitter) likes and bookmarks into a local database, and export them as Markdown, CSV, JSON, and more. Built for the X API v2 Pay-Per-Use era. Never lose a post you loved.
